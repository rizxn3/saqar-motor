// Either delete this file entirely
// OR replace its contents with:

export function middleware() {
  // Remove all middleware logic
  return;
}

export const config = {
  matcher: [], // Empty matcher array means middleware won't run
}